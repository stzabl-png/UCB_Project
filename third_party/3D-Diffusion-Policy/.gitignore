# Local outputs/checkpoints/wandb logs — not part of the vendored code
data/
outputs/
wandb/

# Upstream-bundled third-party assets (Metaworld, rrl-dependencies) — heavy and
# unused for our Baseline1 pipeline. If you need them for a separate experiment,
# clone the upstream repo into a different location.
third_party/

# Auto-generated by `pip install -e .`
**/diffusion_policy_3d.egg-info/
**/__pycache__/
**/*.pyc

# Heavy upstream asset (image), not load-bearing
DP3.png
